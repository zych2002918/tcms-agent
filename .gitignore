# monorepo 根 .gitignore
#
# ⚠️ 关键约定：**构建产物模式必须锚定到根**（`/dist/`、`/build/`），
# 不能裸写 `dist/`。裸模式会匹配任意层级，从而吞掉
# `packages/platform/web/dist/`（该目录是刻意入库的前端构建产物——
# 「任意 clone 即可跑出界面」，见 packages/platform/.gitignore 的说明）。
# 各成员目录内的构建产物由各自的 .gitignore 就近管理。

# ---- Python ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.hypothesis/
.coverage
coverage.json
htmlcov/

# ---- 根级构建产物（锚定，勿裸写）----
/build/
/dist/
/dist_check/

# ---- 运行时产物 ----
server.log
_server.log

# ---- Node（依赖目录；前端产物 packages/platform/web/dist 有意入库，不忽略）----
node_modules/

# ---- uv ----
.uv-cache/

# ---- 离线分发物（不打进 git：uv 本体 ~49 MB / 依赖缓存更大）----
# 它们由打包脚本在生成「新电脑点击即用」压缩包时放入，见 scripts/build_release.py
_offline/

# ---- 本地归档（过期压缩包等）----
# 工作区纪律：清理先归档、不直接删；但归档物不该进 git（旧发布包 20+ MB，留着只会误用）。
# 命名约定 _archive_<日期>/，本次把界面已过期的 tcms-agent-v0.1.0-20260917.zip
# 归档为 _archive_20260920/….zip.stale
_archive_*/

# ---- 编辑器 / 系统 ----
.vscode/
.idea/
Thumbs.db
Desktop.ini

# ---- 本地密钥与设置（绝不入库）----
.env
*.local

# ---- AI 生成用例的编译产物（executor_real 写到 <upstream>/tests/）----
# 正常情况下 agent 跑完会立刻搬进沙箱（见 _exec_worker._relocate_artifact），
# 这里是安全网：万一进程被强杀在搬走之前，也不该把产物提交进仓库。
test_ai_generated*.py
