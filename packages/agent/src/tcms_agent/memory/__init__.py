"""多层记忆（四层的实现状态）。

    层           模块            职责                                     状态
    ----------  --------------  ---------------------------------------  --------
    工作记忆     LangGraph state 当前 trajectory（+ 单条结果截断）          ✅
    情景记忆     journal.py      运行日志（JSONL 派生索引，可召回）          ✅
                recall.py       按目标召回"我以前怎么做的"                  ✅
    语义记忆     platform 知识底座 资产 / 图谱 / 混合检索                     ✅
    程序性记忆   consolidate.py  离线巩固：从历史挖出可复用经验（过门禁）     ✅
                recall.py       召回技能并注入提示词                        ✅

## 一条贯穿的设计纪律：写入必须有据可查

- **运行日志**自动写：它是机器生成的事实记录，不含判断，因此无需审批；
- **长期记忆（技能）**必须过两道门禁 + 人工审批：
  1. 引用门禁：引用的资产 id 必须真实存在；
  2. 证据门禁：必须指出它来自哪些运行，且这些 run_id 真的在日志里。

第 2 条防的是"编一条听起来很对的规律"——那种东西一旦入库，
会被后续召回反复强化，比单次回答出错严重得多。

## 召回通道的诚实口径

用 platform 的字符级哈希通道（与知识底座同一口径），衡量的是**字符重合度**，
**不是语义**。真语义通道属于 R6 的 RAG 升级范围；在补齐之前不把字符重合说成语义。
"""

from .journal import RunJournal, RunRecord

__all__ = ["RunJournal", "RunRecord"]
